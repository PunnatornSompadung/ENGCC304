#include <stdio.h> 

int main() {
    int a, b, c, plus ; //input

    scanf( "%d %d %d", &a, &b, &c ) ; //&string
    plus = a + b + c ;
    printf( "Answer = %d\n", plus ) ; //print/output
    
    return 0 ;
} 
